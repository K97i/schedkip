Create Schedule.svelte for timetable (`<div>` spam)
- Do time math for how many flex nums
- Add ScheduleColumn.svelte

Check for schedule conflicts (use day and timeStart/End)
Add "one-off" parameter to course interface (ex: LASARE courses)